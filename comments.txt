- time: 1:12:27
- gitconfig setup:
[push]
 autoSetupRemote = true
 default= current


-----
declare module "drizzle-kit" {
  interface Config {
    dialect: "postgresql" | "mysql" | "sqlite" | "singlestore";
    schema: string; // Add schema as a valid property
  }
}
-------

-client side action: update page after rendering,
 effect a thing, function the user clicks,function more than show UI
-export object pattern for QUERIES